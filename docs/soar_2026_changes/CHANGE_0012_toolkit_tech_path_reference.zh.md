# CHANGE_0012_toolkit_tech_path_reference

## 1）背景与动机
- 为避免优化方向偏离官方建议，需要在每次优化/定制化阶段显式参考 SOAR 官方技术路径。
- 用户要求将 toolkit 页面 `技术路径指引` 作为必查项写入项目指令。

## 2）SOAR 规则合规性检查
- 本次仅流程/指令更新。
- 不涉及模型、内核和推理实现改动。

## 3）改动前计划
- 在 `.github/copilot-instructions.md` 的规则新鲜度章节中新增 `技术路径指引` 必查要求。

## 4）实际代码改动
- 新增一条要求：在进入优化/定制化阶段前，必须显式复核 toolkit 页面的 `技术路径指引`。

## 5）验证
- 人工检查 `.github/copilot-instructions.md` 是否包含新增条目。

## 6）风险评估
- 无（仅策略澄清）。

## 7）回滚说明
1. 从 `.github/copilot-instructions.md` 删除新增的 `技术路径指引` 要求行。

## 8）下一步建议
- 在后续每个变更提案中，明确记录“采用/不采用了哪些技术路径指引建议”。
